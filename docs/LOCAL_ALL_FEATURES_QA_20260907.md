# 本機全功能與流程 QA：2026-09-07 至 09-08

本次承接商品左側高度、商品註記雙欄及全功能流程驗收需求。實作與測試位於 `Stallorder-Platform-catalog-operations-20260907`、分支 `codex/catalog-operations-20260907`，服務網址為 `http://127.0.0.1:3018`。獨立介面改版仍保留在 `codex/responsive-workflows-20260907`；本次沒有部署正式環境。

## 已修正的問題與連動

| 問題 | 原因與修正 | 驗證 |
| --- | --- | --- |
| 商品左側未切齊右側底部 | 左側高度上限與右側標題、批次工具列及清單分開計算；改用同一個有界 grid，兩側內部捲動 | 768／1440 寬量測兩側底部差距不超過 2px；320／390 保留手機階層 |
| 註記仍只有兩張入口卡片 | 雙欄原先只存在於展開視窗；平板與電腦改成頁面直接呈現群組／選項，另保留放大操作 | 群組選取、單一註記、群組選項、排序、批次套用、共用同步、刪除、匯入衝突與重試 |
| 付款操作斷線後一直處理中 | fetch rejection 沒有釋放 pending；補上錯誤視窗及 finally。回應遺失時保留相同付款操作識別 | 中斷請求、已成功但遺失回應、重試只有一筆交易、七種 mock 結果及退款／對帳 |
| 內用抽獎後無法送單 | 前端與 API 允許即時桌位 QR，但 draw RPC 與送單交易仍有舊排除條件 | 即時內用抽獎、重試、加入零元贈品、送單、KDS、出餐與結帳；外送／預約等原排除保留 |
| 抽獎已關閉仍跳活動提示 | 留存活動的 enabled 被直接用來判斷，未先確認主開關 | Node、Edge 與顧客流程都先檢查主開關；既有活動不再阻擋送單 |
| 贈品折扣快照錯誤 | 商品快照 trigger 將贈品明確設定的 false 覆寫成一般商品折扣設定 | 新贈品保留不可折扣屬性；migration 僅更新 trigger，不對複寫中的歷史訂單執行資料回填 |
| 同桌追加時間少八小時 | Edge 的 PostgREST 回傳不含時區的 UTC 文字，被瀏覽器解讀為本地時間 | 統一回傳含時區 ISO，對照資料庫時間與台灣顯示 |
| 安全驗證錯誤視窗按不到關閉 | 錯誤視窗與降級遮罩疊在一起 | 有明確工作階段錯誤時不再疊加一般降級視窗；關閉後保留重新檢查 |

資料庫變更為 `20260907200000`、`20260907200100`、`20260907200200`，目前僅套用至本機兩個獨立 QA 資料庫。

## 功能啟用與範例

`scripts/enable-local-qa-features.ts` 僅接受本機 3018 與專用資料庫 55722，透過既有管理 API 設定內用、外送、店員外送、外帶自取預約、KDS、列印、付款、折扣、推薦加點、滿額／節慶抽獎、自動產能暫停與恢復、取餐顯示。功能旗標與設定前後值存於 `artifacts/local-qa-features-20260907.json`。

- 商家／店員／廚房／平台管理：[本機登入](http://127.0.0.1:3018/login)。最後切回 development，提供四個本機快速登入入口。
- 商品與註記：[商品管理](http://127.0.0.1:3018/merchant/catalog?organizationId=11111111-1111-4111-8111-111111111111)。
- 原範例：[店員](http://127.0.0.1:3018/staff/aming-chicken)、[廚房](http://127.0.0.1:3018/kitchen?stall=aming-chicken)。先前十筆範例的識別資料仍記於 `artifacts/local-board-orders-20260907.json`。
- 七種付款範例：`artifacts/local-payment-orders-20260907.json`，全部為本機 mock，沒有真實扣款。
- 尖峰流程範例：`artifacts/local-surge-orders-20260907.json`，記錄獨立攤位、訂單狀態、各階段等候時間與直接操作網址。
- 另保留兩組關店中的尖峰範例，每組 12 筆；三個尖峰攤位合計 36 筆，清單與目前營業狀態見 `artifacts/local-surge-examples-20260907.json`。尚未產生訂單的四個測試初始化項目已停用，保留其本機稽核資料。
- 多個範例攤位可供同一帳號選擇；使用上方直連網址可直接進入原攤位。QA 快速登入驗證明確帶入 `next`，避免將正確的多攤位選擇流程誤判成登入失敗。

測試會還原其暫時改動的營業設定、翻譯範圍與模組設定；保留供人工操作的範例另外記錄。沒有重設使用者的原始資料庫或刪除先前十筆範例。

## 流程覆蓋

| 範圍 | 檢查內容 |
| --- | --- |
| 顧客 QR／外帶 | 建立安全階段、點餐、選項、金額、餐具、備註、維護遮罩、重試、原單修改、取消、已確認不可修改、遺失回應不重複建單 |
| 時間協商 | 店員改時間、顧客拒絕、再次提案、接受／拒絕、過期／舊版本／重複操作、取消與確認競爭 |
| 閒置追蹤 | 真實超過六分鐘的等待、121 次讀取、429 冷卻與恢復、不重複跳錯誤視窗 |
| 店員／廚房 | 新單確認、品項製作、完成、出餐、付款與訂單完成分開、同桌追加／合併、折扣授權、清桌、KDS 開關與權限 |
| 現金／列印 | 自有測試班次、收款、退回修正、交班覆核、付款更正、列印認領／失敗／重試、手機與平板操作 |
| 商品／註記 | 分類群組、未分組來源、商品主檔、套餐、庫存差額／回補／售完、批次管理、註記 CRUD／匯入與翻譯 |
| 營業／預約 | 店休與特殊時段、當日最後點餐、含第 N 天預約、既有預約提醒、已印桌位 QR 啟閉模組仍可用 |
| 產能 | 十二筆送單、數量造成的加時、滿載自動暫停、已有 session 的送單卡控、製餐後恢復、人工暫停不被自動恢復、關店與開店 |
| 韌性 | Edge 503 切換 Circuit B、裝置並行工作階段、SSE／Realtime 失敗改用輪詢、離線主機許可、QR 快取唯讀與恢復 |
| 介面 | 320／390／768／1440、三區看板、水平溢出、提示／確認／取消、非中文語系、可關閉與重試的錯誤 |

人工關閉點餐時，重新掃 QR 會進入「目前無法使用此 QR Code」頁，顯示關閉原因並移除送單按鈕；非營業時段仍保留可用菜單時，使用引導前往線上 Menu 的視窗。這兩種狀態的畫面不同，均有測試其送單卡控。

已完成的編譯模式驗證：107 個 Chromium 案例及 1 個 WebKit 版面案例；切回 development 並恢復服務後，再通過 8 項快速登入／公開 Menu／QR／外帶／現金交班／列印／非營業提醒檢查。合計 116 個不同案例，最後一次實際執行皆通過，沒有尚未解決或 flaky 案例。單元測試 527 個檔案、3,040 項通過，原有 2 檔／9 項需要外部環境的測試維持略過；全新 SQL 基準資料庫 71 檔、1,603 項通過。`lint`、`typecheck`、build 與 323 個 TSX 檔案的 UI 控制項掃描通過。正式依賴稽核（`npm audit --omit=dev`）回報 0 個漏洞；本次差異及新增檔案的 Gitleaks 掃描未發現密鑰。

## 證據與重跑

完整執行記錄位於本機 `C:/Users/KY/.codex/visualizations/2026/09/06/01a0761b-0cdb-73e1-82cc-59a3e93d5d66/`。`local-qa-evidence-summary.json` 以案例名稱及專案取最後一次實際執行結果，不將重跑次數加總成測試數；沒有執行的案例不會蓋掉先前證據。可隨分支保存的案例名稱與結果清單為 `artifacts/local-qa-evidence-summary-20260908.json`；原始 trace 可能含本機 session，僅保留本機，不加入 Git。

本機營運資料庫為 `stallorder-catalog-ops-20260907`（55722）；SQL 回歸另使用 `stallorder-qa-regression-20260907`（55732），從頭套用 migrations 與 seed，避免測試的固定基準破壞人工範例。

2026-09-08 最後啟用階段曾因同機記憶體不足逾時；使用者核准暫停 3110／3111，續作時確認兩個服務均已停止。3018 與 Docker 當時也已關閉；Docker 啟動另外遇到兩個無法存取的 AF_UNIX 暫存 socket。保留僅含零位元 socket 的原目錄後重建暫存目錄，Engine 29.7.2 與 55722 資料庫恢復健康。沒有重設或重建營運資料庫；SQL 回歸容器已另行停止以釋放資源。全功能設定重跑成功，原十筆及 36 筆尖峰範例再查核通過，紀錄於 `artifacts/local-qa-final-readback-20260908.json`。

```powershell
npm run lint
npm run typecheck
npm test
npm run ui:audit
npm run build
# development 手動測試服務
node scripts/start-local-qa.mjs --port 3018
```

E2E 使用 `playwright.local-qa.config.ts`，指定 `PLAYWRIGHT_APP_URL=http://127.0.0.1:3018`、`PLAYWRIGHT_REUSE_EXISTING_SERVER=true`；PWA／service-worker 測試使用原始 `playwright.config.ts`。編譯版的公開測試 Turnstile site key 必須在 build 階段提供。不得把這些本機測試環境設定套到正式環境。

## 實際限制

- 這份證據覆蓋列出的角色、流程與錯誤劇本，不代表所有按鈕的每一種排列組合都已窮盡；靜態 UI 掃描也不等於每個按鈕的真實瀏覽器測試。
- 五個資料庫硬鎖中的未完成功能維持不可啟用：候位基礎、新預約基礎、動態 QR 基礎、線上付款基礎、CRM 同意基礎。既有 QR 與外帶自取預約仍可測。
- 壽星抽獎尚無可驗證的會員生日，後端仍拒絕啟用。
- 第三方金流、發票、外送平台、LINE 推播、AI 翻譯、寄信採既有 mock／契約測試；沒有設定真實供應商交易，也不宣稱已驗證真實傳送。
- iPad 鎖屏音效、實體印表機／錢櫃、背景推播須實機及供應商驗證。瀏覽器測試的音效事件、Wake Lock 或列印呼叫不能作為這些能力的證明。
- 所有結果都是本機證據，並無 Staging、DR 或 Production 發布結果。
